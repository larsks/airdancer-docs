<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="" xml:lang="">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes" />
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/css/sp.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div id="header">
    <ul class="menu">
{% assign sections = collections.section | sort: 'data.weight' %}
{% for section in sections %}
  <li {% if title == section.data.title %}class="current" {% endif %}><a href="{{ section.url }}">{{ section.data.navTitle | default: section.data.title }}</a></li>
{% endfor %}
    </ul>
  </div>

  <div id="title">
    <h1>{{ title }}</h1>
  </div>

  <main id="content">
  {{ content }}
  </main>

  <div id="footer">
    Copyright &copy; {% year %} Lars Kellogg-Stedman | 
    <a href="{{ repository.url }}/blob/{{ repository.ref }}/{{ page.inputPath }}?plain=1">View page source</a> |
    <a href="{{ repository.url }}/edit/{{ repository.ref }}/{{ page.inputPath }}">Suggest changes</a>
  </div>
</body>
</html>
